basePath: /api/v2
info:
  contact: {}
paths:
  /health:
    get:
      description: Check if the API is running
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Health check
      tags:
      - health
  /store:
    get:
      responses: {}
    post:
      responses: {}
swagger: "2.0"
